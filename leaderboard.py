def climbingLeaderboard(ranked, player):
    mapped_ranks = set(ranked)
    list_ranked = [x for x in mapped_ranks]
    list_ranked.sort(reverse=True)
    print(list_ranked)
    ranks = []
    index = 0
    # for global_score in mapped_ranks:
    #     if player[index] > global_score:
    #         ranks.append(1)
    #     elif player[index] < global_score:
            

                


    # print(mapped_ranks)

# climbingLeaderboard([100, 100, 20, 20, 4, 2, 1], [1, 2])