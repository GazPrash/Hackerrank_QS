#include<iostream>
#include<vector>
using namespace std;

bool factor ()
{ 
		int n=i.length();
		int m=j.length();
		if(n==m)
			return (i<j);
		
		return n<m;   
}


vector <string> bigSorting(vector<string> unsorted) {
	arr = int[unsorted.size()]
	for (int i = 0; i < unsorted.size(); i++){
		arr[i] = unsorted[i];
	}
	
	sort(unsorted.begin(), unsorted.end(), factor);
}
  

int main(){

}